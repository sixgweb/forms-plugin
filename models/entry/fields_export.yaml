# ===================================
#  Form Field Definitions
# ===================================

fields:
    form:
        label: Form
        type: relation
        emptyOption: -- select form --

    turtle:
        label: Turtle
        dependsOn: form