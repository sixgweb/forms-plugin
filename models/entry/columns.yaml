# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    form:
        label: Form
        relation: form
        select: name

    created_at:
        label: Created
        type: datetime
