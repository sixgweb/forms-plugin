# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true
                
    form:
        label: Form
        type: relation
        emptyOption: -- select form --
