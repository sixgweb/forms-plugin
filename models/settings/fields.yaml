# ===================================
#  Form Field Definitions
# ===================================

fields:
    timeout:
        label: Timeout
        comment: Seconds a user must wait between submissions
        default: 3600
        
    timeout_threshold:
        label: Timeout Threshold
        type: number
        comment: Number of submissions allowed before timeout enforced