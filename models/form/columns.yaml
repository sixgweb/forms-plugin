# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    name:
        label: Name
        searchable: true

    is_enabled:
        label: Is Enabled
        type: switch

    save_entries:
        label: Save Entries
        type: switch
